namespace DataStructures.Immutable.Tree
{
    /// <summary>
    /// Represents a non-root node of a tree.
    /// A node can be either a branch or a leaf.
    /// </summary>
    public interface INonRoot : IImmutable
    {
    }
}